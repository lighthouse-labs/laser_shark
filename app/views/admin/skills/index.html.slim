= render_breadcrumbs

h1 skills
hr

.actions.well
  .btn.btn-primary.btn-sm.create-skill-modal-button data-toggle="modal" data-target="#create-skill-modal"
    | New Skill

table.table
  thead
    tr
      th Content

  tbody
    = render @skills

.modal.fade#create-skill-modal
  .modal-dialog
    .modal-content.create-skill-modal
      .modal-header
        button.close type='button' data-dismiss='modal'
          span aria-hidden='true'
            | &times;
          span.sr-only
            | Close
        h4.modal-title
          |
      = form_tag admin_category_skills_path, method: 'post', id: 'create-skill-modal-input' do
        .modal-body
          .form-group
            = text_area_tag("skill[text]", nil, placeholder: 'enter new skill', required: true, class: 'form-control')
        .modal-footer
          button.btn.btn-default type='button' data-dismiss='modal'
            | Cancel
          = submit_tag 'New Skill', class: 'btn btn-primary create-skill-button'